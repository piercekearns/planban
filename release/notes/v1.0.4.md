# Planban v1.0.4

Planban 1.0.4 makes the verified board URL a durable part of every successful open confirmation.

## Fixed

- `/pb` and `/planban` now always reply with the exact clickable verified board URL, including when the Codex in-app browser opens successfully.
- The tutorial opener follows the same always-linked confirmation contract.
- The MCP launch result explicitly tells consuming agents that the URL is required in their user-facing reply and supplies ready-to-use Markdown.

## Verification

- Added regression coverage across the canonical opener skills, shared protocol, and MCP response contract.
- Verified focused opener, MCP, and launcher tests plus the complete public test/build/audit suite.
